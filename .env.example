# 前端 API 地址（本地开发用 localhost，生产环境改为实际后端地址）
VITE_API_BASE_URL=http://localhost:3001/api

# 服务端允许的客户端域名（多个用逗号分隔）
CLIENT_URL=https://midnightatelier.netlify.app

# 阿里云 OSS 配置
OSS_REGION=oss-cn-hangzhou
OSS_ACCESS_KEY_ID=your_access_key_id
OSS_ACCESS_KEY_SECRET=your_access_key_secret
OSS_BUCKET=your_bucket_name
# 可选：自定义域名（如 CDN 域名），不填则使用默认 OSS 域名
# OSS_PUBLIC_URL=https://cdn.yourdomain.com

# 画廊 OSS 配置（不填则回退到主 OSS 配置）
OSS_GALLERY_BUCKET=interior-masters-gallery
OSS_GALLERY_REGION=oss-cn-chengdu
# 可选：画廊自定义域名
# OSS_GALLERY_PUBLIC_URL=https://cdn.yourdomain.com
